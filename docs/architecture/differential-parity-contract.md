# Differential parity contract

Parity uses frozen normalized observations only: slice, scenario, legacy outcome, core outcome, classification and stable reason. No credentials, SDKs, exceptions, runtime handles or storage objects enter observations. `MATCH` requires verified equivalent semantics; stronger isolated security is `INTENTIONAL_DELTA`; unsupported runtime behavior is `UNVERIFIED`; new migration-controller behavior is `NOT_APPLICABLE`.
